// -----------------------------------------------------------------------------
// 文件: src/rtl/params.vh
// 说明: 全局参数定义
// -----------------------------------------------------------------------------

`ifndef PARAMS_VH
`define PARAMS_VH

// -----------------------------------------------------------------------------
// 参数说明 (用于 RTL 统一配置)
// -----------------------------------------------------------------------------
// ARRAY_ROW / ARRAY_COL:
//   - 脉动阵列物理规模 (Row x Col)，与 DSP 资源相关
// DATA_WIDTH:
//   - 激活/权重数据位宽 (当前 8-bit)
// ACC_WIDTH:
//   - 累加器位宽 (当前 32-bit)
// AXI_DATA_WIDTH:
//   - AXI-Stream 数据位宽 (当前 64-bit)
// DEFAULT_K_DIM:
//   - 默认 K 维度/序列长度配置 (上层可通过 AXI-Lite 覆盖)
// -----------------------------------------------------------------------------

// -----------------------------------------------------------------------------
// 阵列维度定义 (物理维度)
// -----------------------------------------------------------------------------
// 为了适应 Zynq-7020 的 DSP资源 (220个)，我们选择 12x16
// 注意：宏定义不加分号
`define ARRAY_ROW 12
`define ARRAY_COL 16

// -----------------------------------------------------------------------------
// 数据位宽定义
// -----------------------------------------------------------------------------
`define DATA_WIDTH    8
`define ACC_WIDTH     32
`define AXI_DATA_WIDTH 64

// -----------------------------------------------------------------------------
// 默认计数器阈值 (可运行时配置)
// -----------------------------------------------------------------------------
// DeiT-Tiny Hidden Dimension = 192.
// 我们假设每次计算处理一个 Tile 的 K 维度。
`define DEFAULT_K_DIM 192 

`endif
